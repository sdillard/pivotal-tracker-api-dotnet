This library provides programmatic access to the Pivotal API using .net.  Eventually, all the Pivotal APIs will be implemented, but for now it is just the most commonly used items.

To Do:
- Write tests
- Add membership functionality
- More CRUD operations